{
  "devDependencies": {
    "lerna": "^6.6.1"
  },
  "name": "monnorepo",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "start": "lerna run start --scope example",
    "build": "lerna run build --scope firestore-ts-client-builder",
    "publish": "lerna run publish --scope firestore-ts-client-builder"
  }
}
