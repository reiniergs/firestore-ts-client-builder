import { BaseEntity } from "../{{#each parents}}../{{/each}}types";
import { {{#each customTypes }}{{this}},{{/each}} } from "../{{#each parents}}../{{/each}}globals";

export interface {{entityInterface}} extends BaseEntity {
  id: string;
  {{> properties properties=properties}}
}
