import { doc, deleteDoc } from "firebase/firestore";
import { db } from "../{{#each parents}}../{{/each}}firebase";

export type {{entityInterface}}RemoveService = ({{> typedSubcollectionParams}}id: string) => Promise<void>;

const remove: {{entityInterface}}RemoveService = async ({{> subcollectionParams}} id) => {
  const docRef = doc(db, `{{> subcollectionPath}}/{{entityName}}/${id}`);
  return await deleteDoc(docRef);
};

export default remove;
