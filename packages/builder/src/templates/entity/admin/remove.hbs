import { db } from "../admin";
import { RemoveService } from "../types";

const remove: RemoveService = async (id: string) => {
  await db.collection(`/{{entityName}}`).doc(id).delete();
};

export default remove;