import { db } from "../{{#each parents}}../{{/each}}admin";

export type {{entityInterface}}RemoveService = ({{> typedSubcollectionParams}}id: string) => Promise<void>;

const remove: {{entityInterface}}RemoveService = async ({{> subcollectionParams}}id: string) => {
  await db.collection(`{{> subcollectionPath}}/{{entityName}}`).doc(id).delete();
};

export default remove;
